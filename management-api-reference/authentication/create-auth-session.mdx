---
openapi: post /kvm/v2/get/auth
title: Create Auth Session
sidebarTitle: Create Auth Session
---

When using “Try it”, switch the server to `https://session.edisglobal.com` first (this endpoint does **not** need `api_host`; it only works on the session host). Then enter your WHMCS email and password; optionally add `kvm_id` to scope the session to a single KVM.
